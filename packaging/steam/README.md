# Steam packaging (legacy)

DeckLink BT is **Desktop Mode only**. Gaming Mode / Non-Steam shortcuts are no longer installed.

Older installs may still have `~/.local/share/decklink-bt/DeckLink BT` or a Steam Non-Steam entry — remove those manually. Re-running `scripts/install-deck.sh` deletes the legacy launcher files under the install dir.
